# Minimal setup.py for pre-commit hook compatibility
# The actual package is installed from PyPI via additional_dependencies
from setuptools import setup

setup(
    name="langlint-hooks",
    version="0.0.0",
    py_modules=[],
)

